# dataValidationSystem
A data validation system that accepts user input if it meets requirements of a specific criteria.
The main purpose of the system is to prevent incorrect or incomplete information from being accepted. The application uses conditional statements (if, else if and else), regular expressions, string length checks and date validation to provide feedback to the user. Validation feedback is displayed next to the relevant field. 
